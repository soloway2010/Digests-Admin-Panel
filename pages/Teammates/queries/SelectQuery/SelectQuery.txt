SELECT * FROM public."teammates"
WHERE {{ ["p.solovev@sdgroup.ai"].map((e) => e.toLowerCase()).includes((appsmith.user.email || "").toLowerCase())
    ? "user_name ilike '%" + (data_table.searchText || "") + "%'" : "FALSE" }}
ORDER BY "{{data_table.sortOrder.column || 'user_name'}}" {{data_table.sortOrder.order || 'ASC'}}
LIMIT {{data_table.pageSize}}
OFFSET {{(data_table.pageNo - 1) * data_table.pageSize}};